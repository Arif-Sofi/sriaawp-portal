# Build checkpoint

Living briefing for the SRIAAWP feature build. Refreshed at the end of every PR by the `pr-checkpoint` skill. Read this first when starting a PR or resuming after a context reset.

## Stack state (latest first)

- Train base: `feat/auth-rbac` (PR #29, under review), on `feat/db-schema` (#24), on `docs/ws-a-meta` (#23).
- `feat/design-system` — PR #37, closes #36. PR1 DONE.
- `chore/agent-workflow` — PR #39, closes #38. Agent tooling (skills, workflow, checkpoint). DONE.
- `feat/public-shell` — PR #41, closes #40. PR2 DONE (MyUTM portal homepage + app shell + BM/EN).
- `feat/news` — PR #43, closes #42. PR3 DONE (news + memos + audit_log + rbac-matrix.md).
- `feat/takwim` — PR #45, closes #44. PR4 DONE (events schema + public Takwim + cached feed + role views).
- `feat/events-conflict` — PR #47, closes #46. PR5 DONE (conflict checker 11.6 + event creation + override flow).
- `feat/parent-dashboard` — PR #49, closes #48. PR6 DONE (parent dashboard + linked children, audited).
- `feat/admin-users` — PR #51, closes #50. PR7 DONE (admin user/role mgmt, verification, family linking, departments).
- `feat/staff-dashboard` — PR #53, closes #52. PR8 DONE (dept-scoped staff dashboard).
- `feat/documents` — PR #55, closes #54. PR9 DONE (document corpus, bytea storage, acl_keys; document_chunk empty awaiting PR10).
- Gap-backlog train (2026-06-20, frontend wiring audit -> issues #58-#70, on top of `feat/documents`): `fix/center-layout` (PR #56, centers the Tailwind v4 `container`), `chore/readme-supabase-cli` (PR #57, README + Supabase CLI devDep). Then the A-E feature train: PR-A `feat/app-navigation-shell` (PR #71, closes #58/#59/#70) DONE. Remaining: PR-B revalidation (#60), PR-C content mgmt (#61/#64/#65/#68), PR-D events publish/edit (#62), PR-E public content + bug fixes (#63/#66/#67/#69). Each stacks on the previous; base of the train is `fix/center-layout`.
- NEXT: PR-B `feat/mutation-revalidation` (#60), branched off `feat/app-navigation-shell`. Then PR10 `feat/rag-chat` (RAG pipeline per master-plan 11.5), branched off `feat/documents`. PAUSED awaiting the human's Gemini API keys (4 free + 1 paid). A synthetic 4-doc corpus is already seeded (document_chunk empty), so RAG can ingest/chunk/embed those once keys exist; NotebookLM docs are an optional extra.
- Divergence note: master plan specced Supabase Storage for documents; PR9 uses Postgres bytea to avoid an external-key dependency for the demo. Production would use Supabase Storage (worth an ADR later).
- Design direction (locked): both public and role dashboards use the MyUTM green card-grid portal look; theme stays emerald green; reference template at `docs/screencapture-my-utm-my-home-2026-05-05-12_17_36.webp`.
- Full plan: 10 feature PRs — design-system, public-shell, news, takwim, events+conflict, parent, admin-users, staff, documents, rag-chat. Task board tracks them.

## Demo environment

- Hosted Supabase project ref `efpyhlfpyxfauvaynrfw` (PostgreSQL 17). `.env.local` `DATABASE_URL` uses the direct connection (`db.<ref>.supabase.co:5432`); reachable from this machine (IPv6 OK).
- Migrations `0000_auth_rbac_profiles.sql` and `0001_rls_policies.sql` applied. Seed loaded: 56 users (1 admin, 5 teachers, 20 parents, 30 students), 4 roles, 29 permissions, 5 departments.
- Login is magic-link; with `AUTH_RESEND_KEY` blank the link prints to the dev-server console. Seed accounts: `test-admin@sriaawp.test`, `teacher.1@sriaawp.test`, `parent.1@sriaawp.test`.
- Dev server: `npm run dev` on :3000. Stop it before `npm run build` (both use `.next`).
- Helper scripts (untracked, in `scripts/`): `_migrate.mjs` (postgres.js `.simple()` migration runner), `_verify.mjs` (count check), `_dbcheck.mjs` (connectivity). Run: `node -r dotenv/config scripts/<f>.mjs dotenv_config_path=.env.local`. Seed: `DOTENV_CONFIG_PATH=.env.local npx tsx src/db/seed/index.ts`.

## Reusable inventory (consume, do not rebuild)

- RBAC/auth: `@/lib/rbac` -> `getCurrentUser`, `requireUser`, `requirePermission(code)`, `hasPermission(user, code, scope?)`. `@/lib/auth` -> `auth`, `signIn`, `signOut`. Session user: `{id,email,name,roles[],permissions[],deptIds[],status}`, status in ACTIVE | PENDING_VERIFICATION | SUSPENDED.
- DB: `@/lib/db` -> `db` (drizzle over postgres.js, `prepare:false`). Schema barrels in `src/db/schema/*`. Seed catalogue `src/db/seed/catalogue.ts` already defines roles/permissions/departments for every feature.
- Design system (PR1): `@/lib/utils/cn`; `@/lib/utils/result` (`ActionResult<T>`, `ok`, `fail`); `@/lib/i18n` (`Locale`, `translate`, `LOCALE_COOKIE`) and `@/lib/i18n/server` (`getLocale`). Components: `@/components/ui/*` (button, card, badge, input, textarea, select, loading, empty-state, conflict-badge, citation-chip, chat-bubble, file-table, dialog, toast, calendar, date-time-range, form/field) and `@/components/shared/*` (nav, app-shell, breadcrumbs, language-toggle). LanguageToggle takes a `locale` prop. Component gallery at `/ui-preview`.
- Portal (PR2): `@/components/portal/*` (portal-header, portal-search, app-tile, app-shortcuts, portal-section, promo-banner); `@/components/ui/icon` (`Icon`, `IconName`, inline SVG, no deps); `@/components/ui/button` exports `buttonClasses({variant,size})` for link-as-button; `@/components/shared/{site-header,site-footer,auth-header}`; `@/lib/i18n/dictionary` (`ui` dictionary) consumed via `translate`; `@/app/actions/auth` `signOutAction`. Gated layouts wrap children in `AppShell` + `AuthHeader`; public layout uses `SiteHeader`/`SiteFooter`. The green header bar is `bg-primary`. Live public routes: `/`, `/privacy`; placeholder `/takwim` (PR4 replaces).
- Content (PR3): `@/lib/content/queries` (listPublishedPublicNews, listVisibleNews(user), getVisibleNewsBySlug, listAllNews, listVisibleMemos, listAllMemos); `@/lib/pdpa/audit` `writeAudit({actorUserId,action,resourceType,resourceId?,metadata?})`; `@/app/actions/news` + `@/app/actions/memos` (gated by news:author/memo:author, audited, return ActionResult). Tables: news, memo (enum content_visibility public|internal|role_list, visibilityRoles text[]), audit_log. Use drizzle `arrayOverlaps` for the visibilityRoles overlap. Live routes: `/news`, `/news/[slug]`, `/admin/news`, `/admin/memos`. RBAC matrix doc: `docs/phase-1/02-requirements/rbac-matrix.md`.
- Calendar (PR4): `@/lib/calendar/queries` (listPublicOccurrences, listVisibleOccurrences({fromISO,toISO,user}), listBlackouts, plus PR5: listManageableEvents(user), getEventForEdit, listRooms). Tables: room, event (status draft|published|pending_review|cancelled, priority normal|exam, rrule), event_occurrence (materialized; one per event so far), event_audience (audience_type public|role|department + audience_ref), blackout_window (scope school|department, is_hard). Visibility: a published event with a public audience is shown to anyone; role/department audiences only to matching users. Live: `/takwim`, cached `/api/takwim`. Reuse `@/components/ui/calendar`. Display dates in Asia/Kuala_Lumpur.
- Conflict/Events (PR5): `@/lib/conflict/rrule` (expandOccurrences, serialize/parseRrule), `@/lib/conflict/classify` (classifyConflicts — PURE, matrix per 11.6), `@/lib/conflict` `detectConflicts(tx, candidate, occurrences)`. `@/app/actions/events` `createEvent` (outcomes PUBLISHED|PENDING_REVIEW|BLOCKED_HARD; HARD without override persists NOTHING; override requires event:override_conflict; all writes + audit in one tx) and `publishPendingEvent`. `writeAudit(entry, executor=db)` now accepts a tx for transactional audit. Live: `/staff/events`, `/staff/events/new`. Debt: no DB exclusion constraint, so a concurrent-create room race is possible (app-layer detect only).
- Parent/Family (PR6): `@/lib/family/queries` (listChildrenForParent(parentUserId); viewChildForParent({parentUserId,studentUserId}) — verifies family_link BEFORE any student select, audits 'student.view' only on real disclosure, returns null otherwise; never selects ic_no_encrypted). Live: `/parent/dashboard` (real portal: children + news + takwim teasers), `/parent/children`, `/parent/children/[studentId]` (notFound when not linked). Guards PENDING_VERIFICATION + user:read:self.
- Admin (PR7): `@/lib/admin/queries` (listUsersWithRoles, getUserDetail, listPendingParentRequests, listDepartments, listRolesCatalogue, listParentsBrief, listStudentsBrief); `@/app/actions/admin-users` (assignRole, revokeRole, approveParent, rejectParent, linkFamily, bulkLinkFamilyCsv, createDepartment — each requirePermission + audited; approve/reject transactional). Live: `/admin/users`, `/admin/users/[userId]`, `/admin/verify`, `/admin/family-links`, `/admin/departments`. KEY FACT: there is NO `users.status` column — a user's status (ACTIVE | PENDING_VERIFICATION | SUSPENDED) is DERIVED in `@/lib/rbac/session-context` from the latest `parent_verification_request.status` (approve => ACTIVE).
- Navigation/shell (PR-A): `@/lib/navigation` (`ADMIN_NAV`/`STAFF_NAV`/`PARENT_NAV` NavItemDef arrays with per-section `permission: PermissionCode`; `dashboardPathForRoles(roles)` -> admin>teacher>parent dashboard else `/`; `filterNavDefs(defs, user)` hides items the user lacks). Components: `@/components/portal/portal-nav` (PortalNav, container-wrapped tile row, renders null if empty) and `@/components/portal/role-nav` (`<RoleNav area="admin"|"staff"|"parent" user locale />`, permission-filtered + i18n'd) — rendered in each authenticated layout's nav slot beside AuthHeader. `SiteHeader` is now session-aware: takes `user?: {name, dashboardHref}`; layouts pass it from `auth()`. `AuthHeader` takes `homeHref`. `/portal` (`src/app/portal/page.tsx`) is the role-landing redirector; login default callbackUrl is `/portal`; logout -> `/`. To add a nav entry, append to the relevant array in navigation.ts with a real catalogue PermissionCode + a `nav.*` i18n key.
- Documents (PR9): tables document, document_version (file bytes as Postgres bytea), document_chunk (EMPTY until PR10). `@/lib/documents/acl` (computeUserAclKeys, computeDocAclKeys), `@/lib/documents/queries` (listVisibleDocuments(user) via arrayOverlaps on acl_keys, listAllDocuments, getDocumentForDownload — ACL-checked), `@/app/actions/documents` (uploadDocument <=25MB + mime allowlist + visibility-validated, replaceVersion, deleteDocument — gated + audited). Secured download at `/api/documents/[id]/download` (ACL + sanitized Content-Disposition). Live: `/staff/documents`, `/admin/documents`. 4 synthetic text docs seeded. acl_keys: ['public'] | ['internal'] | ['role:<code>'...] + optional ['dept:<id>']; user keys = ['public','internal', role:*, dept:*]. For RAG (PR10): chunk rows copy the doc's acl_keys; pre-filter is `document_chunk.acl_keys && user_keys`.

## Conventions (enforced; the reviewer checks them)

- const only, no reassignment; early returns, max 1 level of if-nesting; max 3 positional params (object beyond; React props exempt); no emojis; minimal comments.
- Tailwind v4 config-less: semantic token utilities only (`bg-card`, `text-muted-foreground`, ...); no raw hex; NO `dark:` variants — theming flips via the `.dark` class (`@custom-variant dark`).
- React 19 compiler on: no forwardRef / useMemo / useCallback. `"use client"` only where hooks or handlers are used.
- Next.js 16: route gating in `proxy.ts` (not `middleware.ts`); mutations via Server Actions returning `ActionResult<T>`; `cookies()` is async.

## Pitfalls — do not repeat

- Prettier: a route-group glob like `src/app/(public)/**` breaks (parens are extglob) and aborts the run; format new files by explicit path. A broad `src/lib/**` reformats unrelated base-branch files — target only this PR's new files.
- Seed/scripts load `dotenv/config`, which reads `.env`, not `.env.local`; pass `DOTENV_CONFIG_PATH=.env.local` (or `dotenv_config_path=.env.local` with `node -r dotenv/config`).
- `Cannot find module` for a dependency listed in package.json -> run `npm install` (lockfile had it, node_modules did not).
- Supabase: use the direct or session connection for the seed (transaction pooler :6543 rejects prepared statements). There is no `db:migrate` script; apply SQL with the `.simple()` runner.
- Subagents may touch files outside scope and may introduce a module-level `let`; review the diff and revert stray changes (for example accidental formatting of base-branch files). Use `crypto.randomUUID()` for ids, not a counter.
- Tailwind v4 `container` is now centered with `padding-inline: 1rem` via an `@utility container` override in `globals.css` (PR #56, `fix/center-layout`, the base of the gap-backlog train). It no longer sits flush-left, but it still has only 1rem of horizontal padding — do not assume large gutters. Any branch that needs this centering must stack on `fix/center-layout` or later.
- A persistent nav row in an authenticated layout must be wrapped in `container` (use `PortalNav`/`RoleNav`); `AppShortcuts` alone has no `container`, so placed in a layout's nav slot it renders flush to the viewport edge.
- A component on a colored bar (`bg-primary`) must use the matching `*-foreground` token, never the base color token (`text-primary` on `bg-primary` is invisible). Prefer opacity for inactive states (`text-primary-foreground/70`).
- `Button` is not polymorphic; for a link styled as a button use `<Link className={buttonClasses({variant,size})}>`, do not hand-duplicate classes.
- Migrations (settled approach): do NOT run `npm run db:generate` — its journal numbering collides with the hand-authored RLS files. Instead update the Drizzle schema files (for ORM types) AND hand-author the migration SQL with the next sequential number (applied so far: 0000, 0001, 0002, 0003, 0004; NEXT is 0005), matching the DDL style of `0002_content_audit.sql`. Leave `supabase/migrations/meta/*` and `_journal.json` untouched so `npm run db:check` stays green. Apply only the NEW file via `node -r dotenv/config scripts/_apply.mjs supabase/migrations/000N_*.sql dotenv_config_path=.env.local`, then re-seed and verify with `scripts/_counts.mjs`. Never re-run 0000/0001 (no IF NOT EXISTS).
- Handle expected DB constraint violations in server actions (unique = SQLSTATE `23505`) and return `fail(msg, {fieldErrors})`; never let them surface as a 500.
- Build Postgres array predicates with drizzle `arrayOverlaps(col, values)`, not string-interpolated array literals.
- Display dates/times in `Asia/Kuala_Lumpur` explicitly (the deploy server tz is UTC): pass `timeZone` to `toLocaleString`/`toLocaleDateString`, and derive any calendar `dateISO` in the same tz.
- A seed insert for a table with no natural unique key needs a presence guard (skip if a row already exists); `onConflictDoNothing()` without a real unique target still inserts duplicates on every re-seed.

## Open decisions / debts

- Dev-only Gemini key rotation approved: 4 free keys plus paid fallback in local dev; single key in prod. ADR-007 addendum to be written in PR10 (rag-chat). Gemini keys and the synthetic NotebookLM documents are still owed by the human before PR10.
- Pre-existing repo-wide Prettier debt (~32 files from earlier PRs) — out of scope for the feature train; a dedicated `style:` pass can fix it.

## PR log

- PR1 (#37, `feat/design-system`): UI design system — Tailwind v4 tokens, cn/ActionResult/i18n, full `@/components` library, `/ui-preview`, ui-design-system.md. Verified green (typecheck, lint, build, tests); independent review plus a11y fixes (useId dialog ids, hydration-safe locale/calendar, aria-current, per-instance preview ids, immutable grid).
- PR2 (#41, `feat/public-shell`): MyUTM portal homepage + app shell + BM/EN. Portal component library, bilingual landing with section empty-states, gated/auth layouts wrapped in AppShell+AuthHeader, privacy page, `/news` `/takwim` placeholders, `buttonClasses`. Verified green (typecheck, lint, build 14 routes, tests); two review passes, fixes applied (green-bar toggle contrast, removed overflowing full-bleed hack, search aria-label).
- PR3 (#43, `feat/news`): news + memos + audit_log + RBAC matrix. Visibility taxonomy (public/internal/role_list), gated+audited server actions, public `/news` + `/news/[slug]`, admin `/admin/news` + `/admin/memos`, RLS, synthetic seed (6 news / 3 memos). Verified green (typecheck, lint, db:check, build 15 routes, tests); migrations 0002+0003 applied to Supabase, counts verified; review confirmed no anonymous leak; slug-collision (23505) and `arrayOverlaps` fixes applied.
- PR4 (#45, `feat/takwim`): events/occurrence/audience/room/blackout schema (hand-authored migration 0004) + public Takwim page + cached `/api/takwim` + role/department-scoped calendar queries + synthetic seed (3 rooms, 8 events, 2 blackouts). Verified green (typecheck, lint, db:check, build 16 routes, tests); applied + counts verified; review found no anonymous leak; timezone pinned to MYT; memo seed idempotency fixed.
- PR5 (#47, `feat/events-conflict`): conflict checker (master-plan 11.6) as pure unit-tested logic + DB detection + createEvent/publishPendingEvent actions + staff event UI (form, conflict modal, list) + design doc. 26 unit tests; verified green (test, typecheck, lint, build 18 routes); adversarial review confirmed override is permission-gated and hard blocks never persist; fixes applied (soft-conflict duplicate, audit-in-tx, NaN guard, i18n).
- PR6 (#49, `feat/parent-dashboard`): parent dashboard + linked-children pages + audited family queries. Verified green (typecheck, lint, build, tests); access-control review confirmed no cross-parent access, audit after linkage, no IC leak; guard + audit-ordering + i18n fixes applied.
- PR7 (#51, `feat/admin-users`): admin user/role management, parent verification, manual + CSV family linking, departments. Verified green (typecheck, lint, build, tests); security review confirmed all 7 mutations permission-gated + audited; fixes applied (deptId validation, revoke filter, primary-contact control, dead keys).
- PR8 (#53, `feat/staff-dashboard`): department-scoped staff dashboard (events/memos/takwim + shortcuts) reusing vetted queries. Verified green (typecheck, lint, build, tests); gate + scoping confirmed.
- PR9 (#55, `feat/documents`): document corpus (document/version/chunk, migration 0005), bytea storage, acl_keys, gated+audited upload/version/delete, secured download route, staff+admin UIs, 4 synthetic docs. Verified green (typecheck, lint, db:check, build 21 routes, tests); migration applied + counts verified; security review confirmed no unauthorized download + enforced size/mime; fixes applied (filename header-injection, visibility validation, version labels).
- 2026-06-20 frontend gap audit: a multi-agent audit of the `feat/documents` surface found a systemic "implemented but never wired to UI" pattern; deduped to issues #58-#70. Layout-centering shipped as PR #56; the A-E feature train fixes the rest. See memory `project_frontend_gap_backlog.md`.
- PR-A (#71, `feat/app-navigation-shell`, base `fix/center-layout`): persistent role navigation (`src/lib/navigation.ts` + `PortalNav`/`RoleNav`), session-aware `SiteHeader` (name/dashboard/logout when authed), `/portal` role-landing redirector (login callbackUrl default `/portal`, logout -> `/`), admin dashboard hub, `/staff/documents` shortcut, `/ui-preview` prod `notFound()` gate. No schema/seed. Verified green (typecheck, lint, test 26pass/2skip, build 26 routes incl. `/portal`); cold code review no blockers, applied 2 minor fixes (permission typed as `PermissionCode`, decl order). Closes #58/#59/#70.
